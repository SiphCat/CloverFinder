/** CloverFinder brand mark (magnifying glass + clover). */
export function CloverLogo() {
  return (
    // eslint-disable-next-line @next/next/no-img-element
    <img
      src="/cloverfinder-logo.png?v=3"
      alt=""
      className="logo-img"
      width={56}
      height={56}
      decoding="async"
    />
  );
}
